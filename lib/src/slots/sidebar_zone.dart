/// Zone within the activity bar where a sidebar slot icon renders.
///
/// [main] items appear in the upper group (alongside session init, files, run).
/// [bottom] items appear in the lower group (alongside settings).
enum SidebarZone { main, bottom }
