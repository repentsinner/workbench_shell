# Example-app artifacts not tracked in git. The example is a minimal
# host and its lockfile churns whenever upstream transitive deps shift;
# leaving it out keeps the example directory a clean source sample.
pubspec.lock
